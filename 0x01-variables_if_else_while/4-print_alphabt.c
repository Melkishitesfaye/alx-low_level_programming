#include <stdio.h>

/**
 * main - prints the alphabet in lowercase, followed by a new line.
 * Return: 0(Success)
 */

int main(void)
{
	char alp = a;

		for ( alp <= 'z'; alp++)
		{
			putchar(alp[i]);
		}
		putchar('\n');
	return (0);
}
