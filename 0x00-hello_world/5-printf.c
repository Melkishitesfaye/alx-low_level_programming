#include <stdio.h>
/**
 * main -A program using printf f to display a string
 * Return: 0 (success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
